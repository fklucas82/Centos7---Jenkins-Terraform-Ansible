---
  - hosts: all
    vars:
      #xi_ip: 'nagios.ia.wd.org'
      ansible_host_key_checking: false
      ansible_python_interpreter: /usr/bin/python3
    become: yes
    become_method: sudo
    roles:
      - role: dnf_update
        tags: ['DNF']
      - role: join_ad
        tags: ['AD']
      # - role: ncpa_install_linux
      #   tags: ['NCPA']
      # - role: register_with_xi
      #   tags: ['XI']
      # - role: install_fireeye
      #   tags: ['FX']
    handlers:
      - name: restart sssd
        service:
          name: sssd
          state: restarted
